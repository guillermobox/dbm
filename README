DBM
===

Database manager to use from the terminal in a very lightweight fashion.

To compile in ubuntu you need `python-yaml`, `libgdbm-dev` and `libgdbm3`,
then do:

    make test

To compile and run the basic test suite.
